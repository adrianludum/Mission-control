# Mission Control — Changelog

- **2026-08-10** — Project created. Prior-art research captured. Prototype v0 (fake data) delivered. Discovery Phase 1 begun.
- **2026-08-10** — Phase 1 core decisions logged (1.1–1.5): re-entry briefing is the v1 spine; agent management deferred to v2 (read-only activity log allowed); briefing content spec agreed; two-level hierarchy (compact tiles → rich project view); capture via per-project .md state files + trigger phrase. PRD first draft synthesised.
- **2026-08-10** — Repo pushed to GitHub (adrianludum/Mission-control); repo files are now the single source of truth across sessions. Phase 2 (Scope & Shape) begun: project = repo (2.1); manual promotion of chat ideas, Claude creates and seeds the repo (2.2). Q2 answered, Q6 narrowed, Q17 added (can a mobile Claude session create a repo?).
- **2026-08-10** — Decision 2.3: curated project index lives in the Mission-control repo, maintained by Claude at promotion/retirement. Q6 fully answered; Mission-control becomes the hub /mission reads first.
- **2026-08-10** — Decisions 2.4–2.6: trigger phrase "update log" (Q13 closed); Mac mini snapshot reporter with loud staleness degradation (Q16 closed); nine-file `.mission/` artefact standard (Q3 closed). Repo migrated to its own standard — state files now live in `.mission/`, TASKS.md and AGENT-LOG.md added, thin root README points in.
- **2026-08-10** — Decision 2.7: synopsis pre-computed at source (`.mission/SYNOPSIS.md`, written at every checkpoint); /mission assembles + re-thinks only stale projects. Standard set becomes ten files. Q5 closed. First SYNOPSIS.md written for this project.
- **2026-08-10** — Phase 2 closed; PRD synthesised to Phase-2 state. Phase 3 (User Journeys) opened: two-band home screen (3.1), launch-pad exit ramp (3.2), planning-incompleteness signal (3.3), continuous checkpoint replaces session-end save (3.4, amends 1.6).
- **2026-08-10** — Phase 3 closed: corrections-in-repo-chats journey (3.5), ceremony-free born-thin promotion (3.6). Four journeys (J1–J4) synthesised into PRD §7. Phase 4 (Technical Shape) opened — spikes Q15/Q17 front of queue.
- **2026-08-10** — Phase 4 closed, PRD synthesised: reconstruction narrowed to git + Claude Code transcripts (4.1, Q15); manual repo birth + "sync with git X" after 403 spike result (4.2, Q17 — supersedes 3.6's zero-ceremony birth); hourly dumb reporter (4.3); /mission renders a visual dashboard artifact with NASA wordmark and tap-to-drill (4.4). PLAYBOOK.md added. Nothing left blocks code.
- **2026-08-10** — Phases 5 & 6 closed, **discovery complete**: worm-era NASA visual direction (5.1); "systems nominal" emotional contract (5.2); four-step build order, two-week line after enrolment (6.1). PRD stamped Complete. 29 decisions in one day. Next: build step 1, the /mission skill v0.
- **2026-08-11** — **Build steps 1–4 executed** in one session via four parallel agents (branch `claude/automated-build-steps-fiyq14`): /mission skill v0 (INDEX.md + SKILL.md + worm-wordmark dashboard template, browser-render-verified); Mac mini reporter (script + plist + INSTALL.md, tested against fake repos — installation is the remaining at-machine step); enrolment tooling (ten-file `templates/mission-seed/` + /enrol skill with pairing-table approval gate — the wave run remains); mission-discipline skill + CLAUDE.md snippet (this repo now carries its own CLAUDE.md — dogfood). Steps 2 and 3 shifted from "Adrian builds at the machine" to "Adrian installs/runs at the machine" — the code is done.
- **2026-08-11** — Scope amendment on first live use (Decision 7.1): the board gets a **standing artifact URL** (`BOARD.md`) that every /mission render republishes to, plus an hourly refresh Routine. "Pinned artifact / scheduled delivery" exclusion narrowed accordingly; /mission skill updated to honour BOARD.md.
- **2026-08-11** — Hourly board-refresh Routine created (7.1 executed): "Mission Control board refresh", :17 UTC hourly, fresh session per fire, republishes the standing URL. Learned: Routine-fired sessions carry no MCP connectors.
- **2026-08-11** — **Tier-2 build questions closed** (Decisions 8.1–8.3): scoring rubric replaces the interim heuristic (Q8); typed analytics column + `snapshot/analytics.md` cache, mapped at enrolment (Q7 — cache design forced by the Routine's connector-less reality); agent activity sourced solely from AGENT-LOG.md with an unlogged-commits cross-check (Q14). Q9 closed by shipped reality (mobile in). /mission SKILL.md + template, /enrol, INDEX.md and snapshot/README.md updated. Remaining open questions are all Tier-3 (beta).
- **2026-08-12** — **Tier 3 closed; the roster replaces the pairing table.** Q10 answered by building the escalation-only watch: `/mission-watch` skill + `snapshot/watch-state.md` (14-day cooling period) + a daily Routine at 07:12 UTC that sends nothing on a good day (9.4). Q11 answered by decision, not build — sharing stays out, strictly personal (9.6). The trust threshold parked since 3.5 is now `TRUST-TEST.md`: four clauses over 14 days, the load-bearing two being "the system saw something first" and "no already-resolved blocker was rendered" (9.5). **Scope change on Adrian's ask**: enrolment moves from a one-shot pairing table to a standing roster — `CANDIDATES.md` of tickable projects, applied by a new `/roster` skill, with retirement defined as removing the board row and nothing else (9.3). Reporter extended to depth-2 scanning: 20 repos → 34, surfacing the nested projects in `events/`, `fitness/` and `rowing/` that were invisible. `/mission` gained §3a: verify machine-checkable blockers against live evidence before rendering them (9.2), after the reporter blocker was rendered for a full day post-resolution. `gh` found authenticated at the mini with `repo` scope, narrowing 4.2 — promotion is now fully automatable there (9.1).
- **2026-08-12** — **Enrolment wave two applied via the roster** (first real `/roster` run, from a cloud session): Adrian named 8 projects; 7 enrolled — ULBC-salesforce, Drone Rowing Analysis, BoatHUB, Race Timing, AI Gym Hub, Gym Hub, Timer App — each with a ten-file `.mission/` seed pushed to its repo and an INDEX.md row. Campaign CRM ticked but held on its recorded blocker (two working copies of one remote; canonical-folder decision is Adrian's). The board goes from 8 to 15 projects. Known drift: BoatHUB's seed sits on session branch `claude/mission-control-roster-update-abe98y` (the cloud session's designated branch), not main — same class of issue as the training_status seed.
- **2026-08-17** — **Two stale-state corrections and one blocker cleared.** Back-filled the unlogged 2026-08-14 run (Timer App → Broadcast Timing App renamed across board, folder and repo; the app's own database-rules lockout found and marked blocked) and the 2026-08-16 `/mission-watch` firing (first two escalations raised, both stale ULBC blockers) — both had committed to hub files while leaving `.mission/` untouched, so neither appeared in the agent log. The database-rules deploy is now a tracked `owner: Adrian` blocker rather than a note inside an INDEX row. **Campaign_CRM collision resolved (9.8)**: `ludum-outreach` is canonical; `ludum-campaigns` retires unmerged, its branch tip being a 92,947-line deletion that the roster's "newest copy wins" framing would have adopted. The BoatHUB merge blocker is annotated as unverified since 2026-08-12 — today's snapshot shows that checkout on `main` and clean, so it may already be stale.
- **2026-08-17** — **The approval bottleneck named and closed by build (9.9).** Adrian's "I do not want to keep allowing steps for Mission Control to work" traced to one cause: no cloud session can push to `adrianludum/*`, so every mechanical repair — stranded seeds, roster ticks, stale index rows — became an `owner: Adrian` task despite needing no judgement. Built `healer/`: a write-side twin to the reporter, one headless Claude session a day at the mini, charter limited to mechanical repair, with flag-probing before and an allowlist audit after. `owner: Adrian` now means judgement, deploys and money only. Cloud sessions stay observers; granting them push rights was deferred because the one that had it seeded onto its own session branch, which is the drift class itself.

- **2026-08-17** — **Audit night: the write side made to work, the observing side made able to see.** A full review of the code, the git layout and the snapshot↔index↔roster agreement found the healer could not have succeeded on its first run — its audit flagged the `INDEX.md` edit its charter demands, and `acceptEdits` never covered the git calls it needed, which was *measured* against the real CLI rather than argued (9.10). The reporter gained file names, an oldest-dirty age, behind-counts and fetch staleness, which is what two of the watch's four escalation conditions always assumed existed (9.11); the counts alone were why three untracked spec documents in AI Gym Hub read as noise for two months. The reporter also took a lock and stopped being able to hang on a credential prompt, and its committed plist stopped pointing at a home directory that exists on no machine. Left undone and handed back: the git-layout tidy — deleting four dead session branches and moving the default off a session branch — needs rights this session does not have.
