# Mission Control — Feedback Log

| Date | Source | Feedback | Status |
|---|---|---|---|
| 2026-08-11 | Adrian (first live use, on plane) | Expected the rendered board to update after the merge ("waiting for hourly update?"). Cause: mental model — the board is a point-in-time render, refreshed only by re-running /mission; the hourly reporter feeds Mac mini state, not board refresh. Not a checkpoint miss. | Logged — feeds Q4/Tier-3 "doors" thinking (auto-refreshing/pinned board is a known non-goal for v1) |
| 2026-08-11 | System (routine-firing verification) | **Duplicate board-refresh Routines created** (:13 and :17 UTC) — a parallel session created the Routine at 01:13 and checkpointed to an unmerged side branch, so TASKS.md on default still read "blocked"; this session, reading default at 01:16, created a second. Cause: checkpoint isolation — state written mid-session is invisible to other sessions until merged to the default branch. Fixed: duplicate (:13, trig_01XiNPEv24C5C96hCXVoErNN) deleted; :17 kept. | Logged — lesson: cross-session state (Routines, external resources) should be verified against the live system (list_triggers), not just .mission files; and checkpoints that other sessions must see belong on the default branch fast |
