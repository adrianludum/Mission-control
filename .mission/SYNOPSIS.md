# Mission Control — Synopsis
*Overwritten at every checkpoint. Written 2026-08-17 (late) by a cloud session — audit night.*

**Where it stands:** The write side now works and the observing side can finally see. Both healer defects are fixed and proven by fixture runs: a legitimate run exits 0 (it used to exit 1 for editing the `INDEX.md` its own charter demands), a rogue edit still exits 1, and the probe now commits in a throwaway repo and reads the commit back — it fails correctly against the old flags, so this class of defect cannot ship again (9.10). The reporter now reports file *names*, an oldest-dirty age, behind-counts and fetch staleness, which is what two of the watch's four escalation conditions always assumed existed (9.11); it also takes a lock and can no longer hang on a credential prompt. Both plists install straight from the repo with no hand-editing.

**Open:** The git tidy is the one job that could not finish, and it is a permissions wall rather than an unknown: this session cannot delete remote branches (classifier) or move the default branch (needs repo admin, no tool exposes it). The exact paste-ready sequence is in TASKS with the four recoverable SHAs, and the merge it depends on was verified conflict-free against the live remote tip. Still true from this morning: 5 of 15 board projects unreadable, 13 no-upstream branches the watch has never raised (now visible to it), Campaign CRM ticked but unindexed.

**Only Adrian can unblock:** (1) the git tidy above — one paste at the mini plus one GitHub setting, order matters; (2) publish the corrected Firebase rules, Broadcast Timing App is still locked out of its own database; (3) answer ULBC's OQ-051, open since April. The healer install is now worth doing — its first watched run should behave, which was not true this morning.
