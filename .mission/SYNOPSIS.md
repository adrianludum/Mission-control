# Mission Control — Synopsis
*Overwritten at every checkpoint. Written 2026-08-17 by a cloud session (full code/git/sync audit).*

**Where it stands:** The observing half is genuinely good — 45 unbroken hourly snapshots, honest staleness degradation, a watch that cools down properly, a self-contained board with no external requests. The repairing half has still never run, and a full audit of it today found two defects that would each stop the first real run: the healer's own audit flags the `INDEX.md` edit its charter demands (hub is row one, allowlist is `.mission/**` only), and `acceptEdits` does not cover the Bash calls it needs to commit or push, with no settings allowlist in the repo to fill the gap. Both are small fixes in code Adrian has not yet installed.

**Open:** Two of the watch's four escalation conditions cannot fire at all — the snapshot records untracked/uncommitted *counts*, no names and no ages — which is exactly why AI Gym Hub's three spec documents never escalated, and why 13 no-upstream branches across 5 repos (including `training_status`'s prepared fix) have been silent since enrolment. The reporter also never fetches, so "unpushed" is as stale as the last manual fetch and *behind* is invisible. 5 of 15 board projects remain unreadable. The hub has no `main` — the default is a session branch, with four dead session branches beside it (diffed today: no unique content, safe to delete).

**Only Adrian can unblock:** (1) publish the corrected Firebase rules — Broadcast Timing App is still locked out of its own database; (2) answer ULBC's OQ-051, open since April; (3) install the healer at the mini — but the two defects above are worth fixing first, since both would surface on the watched first run and cost trust the install is meant to build.
